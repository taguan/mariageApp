<div class="page-content">
    <div id="title"><img src="/assets/images/rsvp.png" alt=""></div>
    <p>Afin que nous puissions préparer au mieux la journée du 11 avril,<br>
     nous vous remercions de répondre à notre invitation avant le 21 mars.</p>
    {{#if globalError}}
        <div class="error">
            Une erreur est survenue...
        </div>
    {{/if}}
    {{#each errors.lastName}}
        <div class="error">
            {{message}}
        </div>
    {{/each}}
    {{#each errors.nbrComing}}
        <div class="error">
            {{message}}
        </div>
    {{/each}}
    <form class="form-style">
        <fieldset><legend><b>Coordonnées personnelles</b></legend>
            {{input type="text" value=lastName placeholder="Nom de famille"}} <br />
            {{input type="text" value=firstName placeholder="Prénom"}} <br />
        </fieldset>
        <fieldset><legend><b>Confirmation</b></legend>
            Je confirme ma présence : {{input type="checkbox" checked=isComing}}<br />
            Nombre de personnes : {{input type="text" value=nbrComing placeholder="Nombre de personnes"}}
        </fieldset>
        <fieldset><legend><b>Commentaire</b></legend>
            {{textarea value=comment cols="80" rows="6" placeholder="Commentaire (Merci de préciser s'il y a un changement relatif à l'invitation reçue)."}} <br />
        </fieldset>
    </form>
    <div class="myButton"{{action 'send'}}>Envoyer</div>
</div>